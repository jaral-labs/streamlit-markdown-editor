# streamlit-markdown-editor

A Streamlit component providing a **dual-mode (WYSIWYG ⇄ raw) markdown editor** — markdown in, markdown out. Edit in a rich WYSIWYG surface or toggle to the raw CommonMark source; both stay in sync over one canonical string.

> **Status: in development.** The distribution name is reserved on PyPI; the Python API is not yet released. This repository currently contains the built editor frontend (Milkdown + CodeMirror 6) and packaging — the `st_markdown_editor(...)` API lands in a future version.

## Requirements

- Python ≥ 3.10
- Streamlit ≥ 1.60 (for the v2 components API)

## License

MIT — see [LICENSE](LICENSE).
